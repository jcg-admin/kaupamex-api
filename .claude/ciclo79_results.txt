CICLO79_RESULTS:
- H-CICLO79-01 [ALTA]: AuditLogView.get() (users/admin_views.py) iteraba
  los tres querysets de eventos (AuthEvent, BusinessEvent,
  UserDeactivationEvent) sin ningun LIMIT en BD, cargando todo el historial
  historico en memoria Python antes de la paginacion. Con millones de filas
  causa OOM en el worker WSGI. Fix: _PER_TYPE_LIMIT=10000 aplicado via
  slicing de QuerySet (LIMIT en SQL) en cada bloque antes de iterar.
  (api@7b634a7)

- H-CICLO79-02 [MEDIA]: AdminUserViewSet.permission_classes = [IsAuthenticated]
  — IsAdminUser ausente al nivel de clase ModelViewSet. Aunque _require_admin()
  protege cada accion en runtime, la ausencia a nivel de framework deja
  OPTIONS/HEAD accesibles a cualquier usuario autenticado (no solo staff) y
  elimina la barrera DRF para futuras acciones sin guard manual. Fix: agregado
  IsAdminUser a permission_classes de la clase. (api@7b634a7)

- H-CICLO79-03 [MEDIA]: OrderSerializer (orders/serializers.py) incluia el
  campo 'id' (PK entero auto-incremental) en la respuesta buyer-facing. Este
  campo permite enumerar todas las ordenes del sistema en orden de creacion.
  Viola la convencion de no exponer PKs secuenciales establecida en H-CICLO76-01
  para payments. Fix: campo 'id' eliminado de fields; order_number (UUID-
  derivado, no secuencial) es el identificador publico correcto. (api@7b634a7)

- H-CICLO79-04: clean — Area 1 newsletter: no existen CampaignDetailView,
  CampaignDeleteView ni CampaignSendView. Solo AdminCampaignCreateView con
  guard H-CICLO78-01. Sin IDOR posible.

- H-CICLO79-05: clean — Area 2 AdminUserDetailSerializer: no expone password,
  tokens ni campos raw. Campos limitados a perfil publico + addresses +
  recent_orders + lifetime_value.

- H-CICLO79-06: clean — Area 3 PaymentSerializer: no incluye gateway_response
  ni raw_body. get_payment_history usa .values() que omite datos del gateway.

- H-CICLO79-07: clean — Area 4 OrderSerializer campos admin: no existe
  staff_notes, internal_id ni cost_price. Totales son Decimal.

- H-CICLO79-08: clean — Area 5 StockAdjustView y VariantStockAdjustView:
  select_for_update() correcto. InventoryDashboardView pagina correctamente
  (ya rastreado H-CICLO50-API3).

- H-CICLO79-09: clean — Area 6 core/middleware.py: no existe archivo. Solo
  middlewares estandar Django/CORS en settings/base.py.

- H-CICLO79-10: clean — Area 7 notifications/views.py: NotificationMarkReadView
  verifica IDOR (user_id check). NotificationMarkAllReadView filtra por
  request.user. Sin NotificationDetailView individual.

- H-CICLO79-11: clean — Area 8 reviews/views.py: ReviewApproveView y
  ReviewRejectView usan transaction.atomic + select_for_update(). Sin race.

- H-CICLO79-12: clean — Area 9 UI account pages: OrdersPage, ReturnsPage,
  SupportTicketsPage leen de slices/hooks correctos. Campos y estados
  alineados con serializers del API.

- H-CICLO79-13: clean — Area 10 admin.py: catalogue/admin.py vacio, orders/
  admin.py no existe, returns/admin.py solo list_display estandar. Sin
  acciones custom ni llamadas externas.

COMMITS: api@7b634a7
