CICLO77_RESULTS:
- H-CICLO77-01 [MEDIA]: CategoryListView N+1 en jerarquías de 3+ niveles —
  _build_category_tree_with_counts() usaba prefetch_related('children') que
  solo populaba el prefetch cache en objetos raíz. Al serializar con
  CategoryWithCountSerializer.get_children(), los objetos hijo en profundidad
  2+ no tenían cache, causando una query DB por categoría en cada cache miss.
  Corregido: se construye children_by_parent desde all_cats y se popula
  _prefetched_objects_cache['children'] en TODOS los objetos antes de
  serializar. (kaupamex-api@4168210)

- H-CICLO77-02: clean — no bug found
  OrderAddressUpdateView acepta campos de dirección en texto plano (sin
  address_id), no asigna una Address del usuario por PK. No hay IDOR.

- H-CICLO77-03: clean — no bug found
  OrderShippingUpdateView: update_shipping_method valida is_active=True y
  estados editables (EDITABLE_STATUSES). No hay bug.

- H-CICLO77-04: clean — no bug found
  ProductImageViewSet no existe en el codebase.

- H-CICLO77-05: clean — no bug found
  apps/users/serializers.py: todos los campos password/token usan
  write_only=True.

- H-CICLO77-06: clean — no bug found
  apps/reports/views.py: todos los endpoints usan _AdminMixin
  (IsAuthenticated + IsAdminUser). Aggregations son server-side via
  build_*_payload(). El export verifica row_count <= 5000 antes de proceder.

- H-CICLO77-07: clean — no bug found
  AdminRefundView: execute_refund() valida amount <= remaining con
  select_for_update() para prevenir race conditions.

- H-CICLO77-08: clean — no bug found
  AdminReportSalesPage.jsx usa PERIOD_OPTIONS predefinidas (today/week/
  month/quarter/year), no tiene campos date_from/date_to.

- H-CICLO77-09: clean — no bug found
  SupportTicketReplyView crea el reply y actualiza ticket.status dentro
  de transaction.atomic(). Patrón correcto.

- H-CICLO77-10: clean — no bug found
  No existe BulkPriceUpdateView en inventory. InventoryDashboardView
  limita page_size a max 200 via min(200, ...).

COMMITS: kaupamex-api@4168210
