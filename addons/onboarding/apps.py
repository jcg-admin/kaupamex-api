from django.apps import AppConfig


class OnboardingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'addons.onboarding'
    verbose_name       = 'Onboarding (progreso de configuración)'
