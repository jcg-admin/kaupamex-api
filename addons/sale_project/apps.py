from django.apps import AppConfig


class SaleProjectConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'addons.sale_project'
    verbose_name       = 'Venta ↔ proyectos (sale_project)'
