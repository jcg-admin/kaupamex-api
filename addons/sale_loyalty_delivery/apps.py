from django.apps import AppConfig


class SaleLoyaltyDeliveryConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'addons.sale_loyalty_delivery'
    verbose_name       = 'Envío gratis como recompensa (sale_loyalty_delivery)'
