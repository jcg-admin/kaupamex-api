from django.apps import AppConfig


class SaleManagementConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'addons.sale_management'
    verbose_name       = 'Gestión de ventas (sale_management)'
