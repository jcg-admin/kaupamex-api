from django.apps import AppConfig


class SaleProductMatrixConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'addons.sale_product_matrix'
    verbose_name       = 'Venta ↔ matriz de variantes (sale_product_matrix)'
