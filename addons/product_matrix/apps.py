from django.apps import AppConfig


class ProductMatrixConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'addons.product_matrix'
    verbose_name       = 'Matriz de variantes de producto (product_matrix)'
