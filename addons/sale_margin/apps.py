from django.apps import AppConfig


class SaleMarginConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'addons.sale_margin'
    verbose_name       = 'Margen de venta (sale_margin)'
