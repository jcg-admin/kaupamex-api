from django.apps import AppConfig
class VoucherConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'apps.modules.voucher'
    verbose_name       = 'Vouchers / Cupones de Descuento'
