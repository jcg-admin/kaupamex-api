from django.apps import AppConfig

class ChartsizeConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'apps.addons.chartsize'
    verbose_name       = 'Variantes de Producto (CHARTSIZE)'
