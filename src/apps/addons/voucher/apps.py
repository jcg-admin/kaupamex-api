from django.apps import AppConfig
class VoucherConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'apps.addons.voucher'
    verbose_name       = 'Vouchers / Cupones de Descuento'
