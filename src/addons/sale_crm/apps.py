from django.apps import AppConfig


class SaleCrmConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'addons.sale_crm'
    verbose_name       = 'Venta ↔ CRM (sale_crm)'
