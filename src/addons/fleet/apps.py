from django.apps import AppConfig


class FleetConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'addons.fleet'
    verbose_name       = 'Flota (fleet.vehicle y bitácoras)'
