from django.apps import AppConfig


class SaleSmsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'addons.sale_sms'
    verbose_name       = 'Venta ↔ SMS (sale_sms)'
