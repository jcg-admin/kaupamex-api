from django.apps import AppConfig


class SalesTeamConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'addons.sales_team'
    verbose_name       = 'Equipos de venta (crm.team)'
