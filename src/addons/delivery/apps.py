from django.apps import AppConfig


class DeliveryConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'addons.delivery'
    verbose_name = 'Delivery (mensajería y guías de envío)'
