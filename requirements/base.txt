# LEGACY / EN RETIRO — iniciativa migrar-api-a-uv-pyproject (T-012).
# Fuente UNICA de verdad de las dependencias: ../pyproject.toml + ../uv.lock.
# Instalar SIEMPRE con `uv sync` (NUNCA `pip install -r`: dispara PEP 668).
# Este archivo se conserva por referencias historicas y se retira tras
# validar `uv sync` en WSL. D-031/H-14.
Django==5.0.1
python-decouple==3.8
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.1
drf-spectacular==0.27.0
mysqlclient==2.2.1
Pillow==10.2.0
cryptography>=42.0.0
mercadopago>=2.2.0
django-cors-headers==4.3.1
