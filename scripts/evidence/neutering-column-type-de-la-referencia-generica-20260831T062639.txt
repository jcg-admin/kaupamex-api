# Control discriminante — column-type-de-la-referencia-generica
# fecha:    2026-08-31T06:26:39
# archivo:  src/orm/fields.py
# anulado:  s/^        '_column_type': ('int4', 'int4'),$//
# tests:    tests/unit/orm/test_field_class_attributes.py

## 1. Verde de partida (guarda presente)
15 passed, 1 warning in 4.84s

## 2. Con la guarda ANULADA — deben caer exactamente los casos que dependen de ella
FAILED tests/unit/orm/test_field_class_attributes.py::TestTheColumnTypeOfEachFamily::test_the_literal_families_answer_their_tuple
FAILED tests/unit/orm/test_field_class_attributes.py::TestTheColumnTypeOfTheTwoTreeDivergences::test_the_generic_reference_keeps_the_integer_column
2 failed, 13 passed, 1 warning in 4.92s

# NOTA sobre el alcance del sed: la expresion borra las TRES declaraciones
# literales de ('int4','int4') — Integer, Many2one y Many2oneReference — asi
# que ademas del caso nuevo cae el de familias literales. El control sigue
# discriminando lo que debe: sin la declaracion, el caso nuevo falla.
