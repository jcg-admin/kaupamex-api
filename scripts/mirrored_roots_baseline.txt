# Archivos nuestros SIN contraparte en la raíz espejada de la referencia,
# declarados con su razón. NO cubre huecos de porte (dirección "a") — esos
# se siguen mostrando siempre, es el punto del gate.
#
# Formato: archivo (relativo al repo)::razón
# Un archivo NUEVO sin contraparte no entra aquí solo: se declara, con razón
# real, o se corrige el sitio. Ver .claude/rules/atributos-de-clase-de-modelo.md
# y check_mirrored_roots.py.
src/orm/fields_nonstored.py::campo no-persistido (store=False); mecanismo propio de la plataforma, no existe símbolo equivalente en el ORM de la referencia
src/orm/inherits.py::mecanismo de _inherits (delegación por Many2one nombrado); construido porque Django no lo trae
src/orm/method_chain.py::encadenado de métodos vía api.depends; construido, no existe en Django
src/orm/routers.py::ruteo multi-DB por company; mecanismo propio de la plataforma multi-tenant
