# Archivos nuestros SIN contraparte en la raíz espejada de la referencia,
# declarados con su razón. NO cubre huecos de porte (dirección "a") — esos
# se siguen mostrando siempre, es el punto del gate.
#
# Formato: archivo (relativo al repo)::razón
# Un archivo NUEVO sin contraparte no entra aquí solo: se declara, con razón
# real, o se corrige el sitio. Ver .claude/rules/atributos-de-clase-de-modelo.md
# y check_mirrored_roots.py.
src/orm/checks.py::VEREDICTO #121 — NO es ORM, es tooling de gate. `$ODOO19C/odoo/orm/` no declara `checks.py` (ls, 2026-08-28) y la referencia no tiene registro declarativo de checks del que portar símbolo: valida el registro vía `_auto_init`/setup al cargar addons. Su hogar natural es `scripts/check_system_checks.py`, hermano de `check_urlconf.py`. 2 importadores. Mover = tarea #483 (decisión del ejecutor)
src/orm/fields_nonstored.py::VEREDICTO #121 — SE QUEDA en src/orm; es un SPLIT de `fields.py`, no una invención. El mecanismo SÍ existe en la referencia (`store` 66 veces en `odoo19c: odoo/orm/fields.py`) pero sin archivo propio. Aquí es archivo aparte por el mismo criterio que las nueve categorías de campo: `orm/fields.py` agrega, no define. El hogar lo apunta desde 2026-08-28. 3 importadores
src/orm/inherits.py::VEREDICTO #121 — SE QUEDA en src/orm; SPLIT de `models.py`. El mecanismo SÍ existe en la referencia (`_inherits` 8 veces en `odoo19c: odoo/orm/models.py`, más `fields.py`, `fields_relational.py`, `model_classes.py`, `registry.py`) y no tiene archivo propio allá. Django lo ata a la jerarquía de clases (MTI); esto es composición por Many2one nombrado. El hogar lo apunta desde 2026-08-28. 2 importadores
src/orm/method_chain.py::VEREDICTO #121 — SE QUEDA en src/orm; mecanismo propio SIN contraparte. La referencia no lo necesita: `_inherit` le construye una MRO real y cada override llama a `super()` (medido: 5 `super()` por archivo en `odoo19c: account_qr_code_{sepa,emv}/models/res_bank.py`). Aquí los overrides cuelgan por `setattr` desde `AppConfig.ready()` y ese idioma no tiene `super()`. Es infraestructura del ORM, así que src/orm es su sitio. 81 importadores — el más consumido de los diez
src/orm/routers.py::VEREDICTO #121 — SE QUEDA en src/orm; mecanismo propio SIN contraparte. Medido: 0 `db_for_read` en toda la referencia — el multi-DB de Odoo es registry-per-DB con `sql_db.db_connect`, no un router. Es la forma Django nativa de ese binding, e infraestructura de ORM. 4 importadores
src/service/retry.py::VEREDICTO #121 — SE QUEDA en src/service; SPLIT de `model.py`. La referencia declara `retrying` DENTRO de `odoo19c: odoo/service/model.py`, sin archivo propio. Aquí el split ya es navegable: `src/service/model.py` apunta a este módulo (3 menciones), que es la forma que #121 fija para los demás. 1 importador (su propio test)
src/tools/log_scrubber.py::VEREDICTO #121 — SE QUEDA en src/tools; mecanismo propio SIN contraparte. Medido: 0 funciones de scrub/redact/mask sobre texto de log en `odoo/tools/*.py` y `odoo/netsvc.py`; lo único próximo es `email_anonymize` (`odoo19c: odoo/tools/mail.py:891`), que anonimiza un correo para envío. Lo exige DEC-LOG-03, no la referencia. 3 importadores
src/tools/logging_context.py::VEREDICTO #121 — SE QUEDA en src/tools; mecanismo propio SIN contraparte. Medido: 0 `ContextVar` en el camino de logging de la referencia (`odoo/netsvc.py`, `odoo/logging.py`, `odoo/http.py`). El correlation_id por request es DEC-LOG-07, decisión nuestra. 6 importadores
src/tools/logging_handlers.py::VEREDICTO #121 — SÍ TIENE CONTRAPARTE, y este gate no puede verla: `PostgreSQLHandler` en `odoo19c: odoo/netsvc.py:47` ("PostgreSQL Logging Handler will store logs in the database"). `netsvc.py` es top-level de la referencia, no un archivo de `odoo/tools/`, así que la comparación raíz-contra-raíz lo declara sin contraparte. El docstring decía "fiel a Odoo la ubica en tools/" y era FALSO; corregido 2026-08-28. Si se mueve a `src/netsvc.py` es decisión del ejecutor. 3 importadores
src/tools/pdf/gen_font_c.py::VEREDICTO #121 — SE QUEDA; ni siquiera es el mismo subsistema. `odoo/tools/pdf/` son envoltorios de pypdf (`_pypdf.py`, `signature.py`); `src/tools/pdf/` es el generador PDF en C con fuentes vendorizadas. Este archivo es un ayudante de compilación (`make`), stdin→stdout, no código de la aplicación
